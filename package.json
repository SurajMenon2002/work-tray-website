{
  "name": "worktray-website",
  "version": "1.0.0",
  "description": "WorkTray — AI-powered browser extension marketing website",
  "scripts": {
    "dev": "npx serve public -p 3000",
    "build": "echo 'Static site — no build step needed. Deploy the /public folder.'",
    "preview": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "keywords": [
    "worktray",
    "chrome-extension",
    "productivity",
    "ai"
  ],
  "author": "WorkTray",
  "license": "MIT",
  "devDependencies": {
    "serve": "^14.0.0",
    "wrangler": "^4.87.0"
  }
}
